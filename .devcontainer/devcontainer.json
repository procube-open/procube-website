{
  "name": "procube-website",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "forwardPorts": [8080],
  "portsAttributes": {
    "8080": {
      "label": "Preview",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y apache2 git-lfs && git lfs install"
}
